const fs = require("fs"); const html = fs.readFileSync("land.html", "utf-8"); const cheerio = require("cheerio"); const $ = cheerio.load(html); $("table.m-bottom-10").each((i, tableEl) => { const trEl = $(tableEl).find("tbody > tr").first(); const npcTd = trEl.find("td").first(); const npcImg = npcTd.find("img").attr("title"); const npcName = npcImg ? npcImg.charAt(0).toUpperCase() + npcImg.slice(1) : $(tableEl).find("thead th").first().text().trim(); const reward = trEl.find("td").eq(2).text().trim(); if (npcName.toLowerCase().includes("raven")) { console.log("npcName:", JSON.stringify(npcName)); console.log("reward:", JSON.stringify(reward)); } });
