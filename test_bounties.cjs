const fs = require("fs"); const html = fs.readFileSync("land.html", "utf-8"); const cheerio = require("cheerio"); const $ = cheerio.load(html); $(".card").each((i, card) => { const title = $(card).find(".card-header h3").text().trim(); if (title.includes("Bounties")) { $(card).find("li.list-group-item").each((j, bEl) => { const choreText = $(bEl).find(".fw-bold").first().text().trim(); if (choreText.toLowerCase().includes("obsidian")) { const rightDiv = $(bEl).find(".d-flex.align-items-center.ms-auto"); console.log("ChoreText:", choreText); console.log("RightDiv Text:", rightDiv.text().replace(/\s+/g, " ").trim()); } }); } });
