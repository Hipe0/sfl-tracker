const fs = require("fs"); const html = fs.readFileSync("land.html", "utf-8"); const cheerio = require("cheerio"); const $ = cheerio.load(html); $("table.m-bottom-10").each((i, tableEl) => { const trEl = $(tableEl).find("tbody > tr").first(); const npcName = $(tableEl).find("thead th").first().text().trim(); if (npcName.toLowerCase().includes("raven")) { $(tableEl).find("tbody > tr").each((j, tr) => { console.log("Row", j, $(tr).text().replace(/\s+/g, " ")); }); } });
